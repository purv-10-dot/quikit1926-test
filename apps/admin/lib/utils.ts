// Re-export from shared package — do not duplicate
export {
  cn,
  formatDate,
  formatDateTime,
  formatRelativeDate,
  generateInitials,
  slugify,
  isValidEmail,
  truncateText,
} from "@quikit/ui";
