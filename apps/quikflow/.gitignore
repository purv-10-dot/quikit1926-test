node_modules
.next
.env
.env.local
.env.*.local
coverage
*.log
*.tsbuildinfo
.turbo
.vercel

# Playwright — build artifacts + minted session-cookie storageState (contains
# a live NextAuth JWT; never commit).
playwright-report
test-results
__tests__/e2e/.auth
