import { SavedFiltersIndex } from "./_components/saved-filters-index";

/** "View all filters" — table of the caller's own + org-shared saved filters. */
export default function FiltersIndexPage() {
  return <SavedFiltersIndex />;
}
