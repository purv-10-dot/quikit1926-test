# Loaded ONLY by `npm run worker:test` (after .env.local).
# Forces automation-only mode: the LeadSquared sync/inbound/poll workers do NOT
# start, so testing automations against the autotest DB cannot push to the real
# LeadSquared account. See docs/claude-project/AUTOMATION-WORKER-OPERATIONS.md.
WORKER_AUTOMATION_ONLY=1
