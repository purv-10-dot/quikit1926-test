node_modules
.next
.env
.env.local
.env.*.local
coverage
*.log
*.tsbuildinfo
.turbo
.vercel

# Local repo-archive snapshot â€” never commit (flagged during backup triage)
QuikCRM_separately.zip

UAT CODE.zip

# DB dumps / backups — real data, never commit
backups/
*.dump
docs/claude-project/mockups/*.sql

# Deploy artifacts (never commit)
*.zip
uat_source.zip
