/**
 * Client-safe entry. Types + constants only — safe to import from browser code.
 * For publishing / token signing (Node-only), import from `@quikit/realtime/server`.
 */
export * from "./contract";
